/**
 * 発表デモは RecordingScreen 内の async ループで行う。
 * 実GPS計測に戻すとき用のプレースホルダ。
 */
export function useRecordingSession() {
  throw new Error(
    "useRecordingSession は発表デモでは未使用です。RecordingScreen のデモ実装を使ってください。",
  );
}
